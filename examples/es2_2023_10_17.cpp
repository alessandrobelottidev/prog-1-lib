#include "../include/magic/magic.hpp"

using namespace std;
using namespace magic;

/*
 * MCD, Massimo Comune Divisore ricorsivo
 * Scrivere un programma che prenda in input due interi e calcoli il Massimo Comune Divisore (MCD) tramite funzione ricorsiva usando l'algoritmo di Euclide. MCD(m, n) = MCD(n, m % n), given that m > n
*/

int main() {
    return 0;
}
