#include "../include/magic/magic.hpp"

using namespace std;
using namespace magic;

/*
 * Divisione custom (senza usare /)
 * Dati in input due interi, scrivere un programma che faccia la divisione tramite una funzione; la funzione restituisce tramite valore di ritorno il quoziente e tramite parametro per riferimento il resto. In questo programma non si può usare il simbolo '/' per la divisione
*/

int main() {
    return 0;
}
